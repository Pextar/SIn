{
  "name": "sin-signer-pwa",
  "version": "0.1.0",
  "private": true,
  "description": "SIn — a passwordless, passkey-gated nostr/bitcoin-style signer as an installable PWA.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "interop": "node test/interop.mjs"
  },
  "dependencies": {
    "nostr-tools": "^2.23.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
